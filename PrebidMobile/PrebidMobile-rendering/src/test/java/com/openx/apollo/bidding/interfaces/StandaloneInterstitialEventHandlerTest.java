package com.openx.apollo.bidding.interfaces;

import org.junit.Before;
import org.mockito.MockitoAnnotations;

public class StandaloneInterstitialEventHandlerTest {
    @Before
    public void setup() {
        MockitoAnnotations.initMocks(this);
    }
}